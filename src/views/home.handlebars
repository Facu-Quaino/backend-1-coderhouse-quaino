<h1>Listado de productos</h1>

<div class = "cards-container">
    {{#each productos}}
    <div class="card">
        <p>{{this.title}}</p>
        <p>{{this.description}}</p>
        <p>{{this.price}}</p>
    </div>
    {{/each}}
</div>

